# Enrollment Portal — environment
# No backend is wired yet: the app runs entirely on a localStorage mock API.
# These land here when real integrations are added (see PLAN.md §12).

# ibl.ai SSO (wired later via /iblai-vibe-auth)
# NEXT_PUBLIC_MAIN_TENANT_KEY=
# NEXT_PUBLIC_API_BASE_URL=https://api.iblai.app
# NEXT_PUBLIC_AUTH_URL=https://login.iblai.app
